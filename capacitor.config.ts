import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'insta-lookalike',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
